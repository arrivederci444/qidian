# 启点乐队招新页

社团乐队"启点队"手机竖屏幻灯片式招新页。

- 打开 `index.html` 即可浏览（招新主页面）
- `role.html` 是职位介绍页
- 演出照片放在 `picture/`，相册列表在 `script.js` 的 `photos` 数组中
- 报名数据写入 Supabase，导出脚本见 `export_signups.py`（本地运行，勿上传）
- 部署到 GitHub Pages（见 `.github/workflows/pages.yml`）
